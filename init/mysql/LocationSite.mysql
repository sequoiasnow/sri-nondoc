DROP TABLE IF EXISTS location_site;
CREATE TABLE about_description (
    id INT(11) NOT NULL AUTO_INCREMENT,
    title VARCHAR(50) NOT NULL,
    coordinates VARCHAR(100) NOT NULL,
    description VARCHAR(1500) NOT NULL,
    PRIMARY KEY( id )
);
