DROP TABLE IF EXISTS about_persons;
CREATE TABLE about_persons (
    id INT(11) NOT NULL AUTO_INCREMENT,
    name VARCHAR(100),
    imagePath VARCHAR(255),
    description VARCHAR(1500),
    PRIMARY KEY(id)
);
